apiVersion: v1
kind: Service
metadata:
  name: mdforge-service
  namespace: default
  labels:
    app: mdforge
spec:
  selector:
    app: mdforge
  ports:
  - name: http
    port: 80
    targetPort: 8000
    protocol: TCP
  type: ClusterIP
